# core api
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-dotenv==1.0.1

# llm + embeddings + reranker
anthropic==0.39.0
voyageai==0.4.1
cohere==5.11.0

# retrieval
rank-bm25==0.2.2
numpy==1.26.4

# database
psycopg[binary]==3.2.3
pgvector==0.3.6
sqlalchemy==2.0.36

# ingestion
feedparser==6.0.11
trafilatura==1.12.2
prefect==3.1.0
httpx==0.27.2

# memory (only piece of llamaindex we're keeping)
llama-index-core==0.11.20

# stats
soccerdata==1.9.0

# observability + cache
langfuse==2.53.0
redis==5.2.0

# evals
ragas==0.2.6

# dev (these get installed in prod too — render's free plan doesn't
# distinguish, and they're small)
pytest==8.3.3
ruff==0.7.2